# Rename Project from img2svg to Vektro

Started 2026-08-26 22h38. Planned; awaiting user execution & approval.

## Context & Objectives

The project is being renamed from `img2svg` to **Vektro** (or `vektro` in code/binary contexts) to establish a distinct, modern brand identity.

---

## 1. Rust Crate & Library Rename

### Cargo.toml & Cargo.lock
- Update package name: `name = "vektro"` in `Cargo.toml`.
- Update binary target / library names if defined.
- Run `cargo check` to refresh `Cargo.lock`.

### Source & Test Imports
- Update module references and tests:
  - `src/lib.rs`, `src/cli.rs`
  - All test files in `tests/*.rs` (e.g. `use img2svg::...` -> `use vektro::...`).

---

## 2. Frontend & Web UI Branding

### Web Package & Config
- `web/package.json`: update name to `"vektro"`.
- `web/index.html`: update `<title>` to `Vektro — Vectorize images to SVG`.
- `web/vite.config.js`: update header banner / title if present.

### UI Components
- `web/views/Header.jsx`: update brand logo text and title to **Vektro**.
- `web/views/Footer.jsx`: update brand footer references to **Vektro**.
- `web/services/worker.js`: update worker messages / logs if any mention `img2svg`.

---

## 3. Documentation & Scripts

- `README.md`: update title, CLI usage (`vektro input.jpg -o output.svg`), build instructions, and docs links.
- `docs/*.md`: update CLI, library, and development documentation.
- `scripts/*.sh`, `scripts/*.mjs`: update script references from `img2svg` to `vektro`.

---

## Status

Completed

---

## Walkthrough (2026-08-26 22h44)

### Implemented Changes

- **Rust Crate & Library**:
  - Renamed package, library, and CLI binary from `img2svg` to `vektro` in [`Cargo.toml`](file:///Users/joshua/dev/joshua/img2svg/Cargo.toml).
  - Updated CLI command name and help messages in [`src/cli.rs`](file:///Users/joshua/dev/joshua/img2svg/src/cli.rs).
  - Updated crate documentation in [`src/lib.rs`](file:///Users/joshua/dev/joshua/img2svg/src/lib.rs).
  - Updated module imports across all integration tests in `tests/*.rs` from `img2svg::*` to `vektro::*`.

- **Web Frontend**:
  - Updated package name to `vektro-web` in [`web/package.json`](file:///Users/joshua/dev/joshua/img2svg/web/package.json).
  - Updated page `<title>` and `<meta>` description in [`web/index.html`](file:///Users/joshua/dev/joshua/img2svg/web/index.html) to **Vektro**.
  - Updated brand title in [`web/views/Header.jsx`](file:///Users/joshua/dev/joshua/img2svg/web/views/Header.jsx) to **Vektro**.
  - Updated repository link in [`web/views/Footer.jsx`](file:///Users/joshua/dev/joshua/img2svg/web/views/Footer.jsx).

- **Documentation & Helpers**:
  - Updated project title, CLI binary syntax, library examples, and links in [`README.md`](file:///Users/joshua/dev/joshua/img2svg/README.md).
  - Updated documentation files in `docs/` and helper scripts in `scripts/`.

### Verification

- `cargo check --tests`: Clean compilation.
- `cargo test`: All 21+ unit & integration test suites passed (100% green).
- `npm run lint`: 0 warnings, 0 errors.
- `npm run build`: Web build completed successfully.

### Suggested Commit Message

`refactor: rename project and crate from img2svg to Vektro`

